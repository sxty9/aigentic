{
  "service": "aigentic",
  "version": 1,
  "categories": [
    {
      "id": "compute",
      "label": "Compute",
      "description": "Local model context budget. single-gpu keeps the context window compact so models stay lean and hot (best latency); multi-gpu allows a large context window that may span both GPUs (for long documents).",
      "settings": [
        {
          "id": "gpuMode",
          "label": "GPU mode",
          "type": "enum",
          "options": ["single-gpu", "multi-gpu"],
          "default": "single-gpu"
        }
      ]
    }
  ]
}
