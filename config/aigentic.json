{
  "service": "aigentic",
  "version": 1,
  "categories": [
    {
      "id": "compute",
      "label": "Compute",
      "description": "Maximum context window for local (ollama) models. aigentic already sizes each request's context to what it needs; this only caps it. compact (~12k tokens) keeps the VRAM footprint lean so models stay hot with the best latency; large (~32k tokens) allows long documents at the cost of more VRAM (which may span both GPUs, slower without NVLink).",
      "settings": [
        {
          "id": "contextMode",
          "label": "Context window",
          "type": "enum",
          "options": ["compact", "large"],
          "default": "compact"
        }
      ]
    }
  ]
}
